## Security Review — 2026-04-23 (scope: paths)

**Summary:** Review of 8 paths covering the WS endpoint (`daydream/api/ws.py`), the four core skills (`daydream/skills/core.py`), the Toon read helpers (`daydream/toons.py`), migrations 006 (seed Rook NPC) + 007 (`toons.presence_text` column + Rook's greeting), and the three test files (`tests/test_db.py`, `tests/test_skills.py`, `tests/test_ws.py`, `tests/test_ws_forge.py`). The increment adds the first hand-authored NPC (Rook, slot 100 at `r-forge`), gives NPCs an optional `presence_text` greeting, and wires the WS broadcast loop to emit that greeting as a `narrate` event after a controlled-toon move into the NPC's room. Net: no new player-controllable input surface, no new LLM prompt surface, no new network endpoints. All SQL in scope is parameterized (`toons.get_toon`, `get_toons_in_room`, `find_toon_in_room_by_name`, `set_current_room`); both migrations are static DDL/DML with no substitution. The WS auth gate remains intact (`auth.is_authed(ws.scope.get("session", {}))` at `ws.py:273`); tailscale-mode bypass is the accepted-risk posture documented in the prior entry. `_emit_npc_presence_narrates` (ws.py:188-217) iterates `get_toons_in_room(room_id)` for the room the controlled toon just entered, filters out the controlled toon's own id, and skips `NULL`/empty/whitespace-only greetings — no cross-room leakage, kicked toons already excluded by the room-scoped query. `presence_text` values are operator-authored in migration 007 (Rook's greeting is a static string in SQL) and flow into a `narrate` payload, not into any LLM prompt template, so no prompt-injection surface is introduced. `_handle_input` still routes canonical-skill text through the room-filtered candidate list (`registry.list_available_for_room(room_id)`), preserving the context-predicate gate. `core.go` gates navigation on `room.exits.get(direction)` so a player cannot jump into a room not reachable via a declared exit. `core.examine`'s new toon branch narrates `toon.seed` + `toon.appearance_seed` + `toon.name` — all three are operator-authored columns, not player-mutable via any in-scope code path (the allowlisted `set_mood` effect mutates only `mood`, confirmed in `daydream/skills/effects.py:128-151`). `Toon.from_row` uses a try/except around `row["presence_text"]` to degrade to `None` on pre-007 DBs, which is a compat concern rather than a security concern. Git history for all 8 files shows no secrets; `-S` search over the non-test files returned nothing. No PII in scope — Rook and Wren are fictional character names.

### Findings

No security issues identified.

### Accepted Risks

- Cookie `https_only=False` in `daydream/server.py:38` is documented inline ("friend-scope; box is on a private LAN/Tailscale only").
- No CSRF token on `/api/login` and `/api/logout`. `SessionMiddleware` defaults to `SameSite=Lax`, which blocks cross-site POST from carrying the session cookie; the worst-case residual impact under the documented threat model is forced-logout from a same-site phishing page, consistent with friend-scope posture.
- The 100.64.0.0/10 hardcoding in `daydream/api/access.py:25` is correct because Tailscale's CGNAT range is fixed by their design. A self-hosted Headscale with a custom range would need that constant updated. Documented in CLAUDE.md.
- `AccessMiddleware` reads `scope["client"][0]` directly rather than honoring forwarded-for headers. This is the secure default given uvicorn is not started with `--proxy-headers`. Operators who later put the app behind a reverse proxy must add `--forwarded-allow-ips` themselves AND audit that the proxy strips inbound `X-Forwarded-For`.
- `bin/game` sources `.env` and `~/.config/daydream/secrets.env` via `set -a; source`; a writable env file is equivalent to arbitrary code execution. Operator-controlled, gitignored, standard practice.
- `bin/game cmd_logs` passes `$1` through `_log_file` without validation; an operator typing `bin/game logs ../../etc/passwd` would resolve into `$RUNDIR/../../etc/passwd.log`. Operator-invoked only; anything the operator can `tail` through this path they can already read directly on the box.
- `bin/qpeek-bootstrap` clones `https://github.com/peterzat/qpeek` and runs `pip install -e` against it. Supply-chain dependency on the project owner's own GitHub account; same trust boundary as the rest of the dev toolchain.
- `/cache/{world}/{target_kind}/{target_id}/{filename}` in `daydream/server.py:91` is intentionally unauthenticated so pre-auth `<img src>` fetches render. `AccessMiddleware` is the real gate; segment validation blocks traversal.
- Tailscale-mode `POST /api/login` at `daydream/api/auth.py:32-34` short-circuits to `authed=True` + redirect without checking the password. Deliberate: `AccessMiddleware` has already CGNAT-gated the request, so the password check would be redundant ceremony. Documented in the module docstring and in CLAUDE.md "Auth" + "Network access" sections.
- Data-skill `set_mood` effect handler (`daydream/skills/effects.py:128-151`) accepts an LLM-chosen `toon_id` without scoping it to the actor's world / room / ownership. In v0 single-player + friend-scope this sits inside the existing trust boundary — an authenticated player can already mutate state through play; the LLM is not a distinct actor against them. v2's per-effect jsonschema + target-authorization pipeline is tracked under BACKLOG `skills-authoring-and-security`. Documented in `daydream/skills/effects.py` module docstring.

---
*Prior review (2026-04-23, paths, commit 1a92aaa): refresh of world-admin CLI + LLM safety + data-skill loader + migration 005 + companion tests; no findings. The c33fdaa hardening commit (narrative-text scan covers `mood`, case-insensitive `wrap_player_input` close tag, `skills.description` persistence) tightened the existing safety surface. All SQL parameterized, Jinja SandboxedEnvironment still guards authored templates, regex banlist patterns have no ReDoS backtracking.*

<!-- SECURITY_META: {"date":"2026-04-23","commit":"1b045a66576e4c55f214ec51d1d390bc737c784a","scope":"paths","block":0,"warn":0,"note":0,"scanned_files":["daydream/api/ws.py","daydream/skills/core.py","daydream/toons.py","migrations/006_first_npc.sql","migrations/007_npc_presence.sql","tests/test_db.py","tests/test_skills.py","tests/test_ws.py","tests/test_ws_forge.py"]} -->
