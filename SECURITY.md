## Security Review — 2026-05-07 (scope: paths)

**Summary:** Path-scoped review of the world-bootstrap-opus landing (commit `dd983fa`). Three files reviewed: `daydream/admin.py` (added `cmd_world_bootstrap` CLI dispatch), `daydream/llm/bootstrap.py` (new `bootstrap_world` pipeline), `tests/test_bootstrap.py` (new hermetic test suite). The new attack surface is the `bin/game world bootstrap NAME --aesthetic "..."` CLI subcommand, which is an operator-only tool that calls Anthropic Claude Opus 4.7 via `litellm.acompletion`, validates the returned JSON against a strict schema, and INSERTs the bootstrapped content into a fresh SQLite file. Operator-trust class throughout: no network endpoint added, no new auth surface, no dependency added (litellm is already pinned for `daydream/llm/client.py`). API key contract: `ANTHROPIC_API_KEY` sourced from env via litellm convention; same trust class as the existing `DAYDREAM_LLM_API_KEY` env-sourcing pattern. No secret values are embedded in source or tests; `git log -p --follow` over all three files surfaces only documentation references to `ANTHROPIC_API_KEY` — never a value. SQL: every INSERT in `_write_db` (worlds, rooms, toons, items, skills) and every DELETE in the wipe-w-bunny preamble uses parameterized queries; LLM-authored strings (room titles, toon names, item descriptions, skill prompt templates) are bound parameters, never interpolated. The slug derivation at line 392 / 424 (`re.sub(r"[^a-z0-9-]+", "-", ...)`) operates on `world_display_name` and toon names before they become string literals in id columns — even though the values flow through bound parameters anyway, the regex collapses anything outside `[a-z0-9-]` so injection attempts in toon names cannot smuggle metacharacters into the id construction. Operator inputs (`--output`, `--model`, `--aesthetic`, `--name`, `--force`) are CLI-supplied and operator-controlled; same trust boundary as `bin/game world archive`'s operator-supplied paths and `--model` overrides in `daydream/llm/client.py`. LLM-authored content downstream rendering: `presence_text` is rendered client-side as `textContent` in `web/assets/main.js:93` (safe); toon `name` flows through `escape()` in the slot panel innerHTML paths (`main.js:211, 218, 225`) and `textContent` for the room toons sidebar (`main.js:37`); `prompt_template` flows into `daydream/skills/data.py:291` where the existing `SandboxedEnvironment` Jinja-2 sandbox renders it — same trust class as hand-authored skills installed via `bin/game world skill add` (the `cmd_skill_add` adjacent in `daydream/admin.py`). The strict envelope validator (`_validate_envelope`) enforces type and shape constraints on all LLM-returned fields so an LLM-authored prompt cannot expand the schema (e.g., add a non-narrate `effects_schema.allowed_kinds` entry that bypasses the existing effect-handler authorization). The validator's exit-graph check (every (A → B) edge requires (B → A)) is a correctness guard, not a security one. CVE-2007-4559 surface is not introduced by this change: bootstrap.py does not extract tar archives; the existing `cmd_restore` hardening in admin.py (lines 297-309) is unchanged. Output-path handling: `Path.expanduser().resolve()` then `unlink()` if `force=True`, then `db.open_db()` runs `path.parent.mkdir(parents=True, exist_ok=True)` — a malicious operator-supplied path could create directories or overwrite files anywhere the invoking user can write, but the operator IS the trust boundary (consistent with the documented "writable env file is equivalent to arbiter code execution" accepted risk). Test file `tests/test_bootstrap.py` uses `unittest.mock.patch` and `AsyncMock` to substitute `litellm.acompletion` at the module boundary; no real network call ever fires from the suite. Test fixtures contain only fictional names ("Wren", "Linden", "Marsh", "Tinker"); no PII. The `_USER_PROMPT_TEMPLATE.format(aesthetic=..., name=...)` string-format is plain Python `str.format` over operator-supplied strings — operator-trust class, no template-injection vector since `format` is not Jinja and `{aesthetic}` / `{name}` are positional. The default `--model` is `anthropic/claude-opus-4-7`; an operator could supply a different provider prefix and litellm would resolve a different env-var key (e.g., `openai/...` → `OPENAI_API_KEY`), but that's the same operator-trust class as any CLI that accepts a model identifier. Per skill instructions, no accepted risks are re-flagged here.

### Findings

No security issues identified in the reviewed scope.

### Accepted Risks

Carried forward from prior reviews; none re-flagged this scope:

- Cookie `https_only=False` in `daydream/server.py:42` (friend-scope; LAN/Tailscale only).
- No CSRF token on `/api/login` and `/api/logout`. `SameSite=Lax` blocks cross-site POST from carrying the session cookie; worst case under the documented threat model is forced-logout from a same-site phishing page. The `POST /api/slots/{slot}/{create,claim,kick}` endpoints inherit the same posture (no CSRF token); same-origin-only state changes from a tailnet-resident attacker page are blocked by `SameSite=Lax` for the `daydream_session` cookie, and unauthed cross-site POSTs from outside the tailnet are 403'd by `AccessMiddleware` before reaching these handlers. Multi-user differentiation that would tighten this lands with v2.
- The `100.64.0.0/10` Tailscale CGNAT hardcoding in `daydream/api/access.py`. Self-hosted Headscale with a custom range would need this updated. Documented in CLAUDE.md.
- `AccessMiddleware` reads `scope["client"][0]` directly rather than honoring forwarded-for headers. Secure default given uvicorn does not start with `--proxy-headers`. Operators putting the app behind a reverse proxy must add `--forwarded-allow-ips` AND audit that the proxy strips inbound `X-Forwarded-For`.
- `bin/game` sources `.env` and `~/.config/daydream/secrets.env` via `set -a; source`; a writable env file is equivalent to arbiter code execution. Operator-controlled, gitignored, standard practice. The `DAYDREAM_DRIFT_*` and `DAYDREAM_MEMORY_*` env vars sit inside this same trust boundary; their parsers float-/int-coerce with a `ValueError` fallback to the default and never propagate to security-sensitive code paths.
- `bin/game cmd_logs` passes `$1` through `_log_file` without validation. Operator-invoked only.
- `bin/qpeek-bootstrap` clones from `github.com/peterzat/qpeek` and runs `pip install -e`. Same trust boundary as the rest of the dev toolchain.
- `/cache/{world}/{target_kind}/{target_id}/{filename}` in `daydream/server.py:118-130` is intentionally unauthenticated so pre-auth `<img src>` fetches render. `AccessMiddleware` is the real gate; segment validation blocks traversal.
- Tailscale-mode `POST /api/login` at `daydream/api/auth.py:46-50` short-circuits to `authed=True` without checking the password. `AccessMiddleware` has already CGNAT-gated the request. Documented in module docstring + CLAUDE.md.
- Data-skill `set_mood` effect handler accepts an LLM-chosen `toon_id` without scoping it (`daydream/skills/effects.py:128-151`). v2's per-effect jsonschema + target-authorization pipeline is tracked under BACKLOG `skills-authoring-and-security`. The `daydream.toons.set_mood` helper itself is a parameterized UPDATE; the unscoped-target risk lives at the effect-handler call site, not in the helper.
- `bin/vllm-bootstrap` and `bin/memory-bootstrap` interpolate `$MODEL` into an unquoted heredoc that becomes Python source. Same operator-trust class as `bin/game`'s env sourcing.
- Stored prompt-injection via captured memory text bypasses `wrap_player_input` containment (`daydream/skills/data.py:362`, `daydream/memories.py:175-209`, `skills/{rook,iris}.json:6`, plus the parallel drift instance at `daydream/drift.py:179-185`). Capture-time banlist is in place; `<memory>...</memory>` separator wraps the rendered text; `</memory>` substrings are NOT neutralized at retrieval time. Realistic impact at v0 is bounded to mild voice/tone deviation, not auth bypass / RCE / data exfiltration. Deeper fix is v2's `skills-authoring-and-security`.
- `/status/drift` in `daydream/server.py:60-80` is unauthenticated by design (no session check; only `AccessMiddleware` gates it). Returns three integer counters with no PII or secrets; in tailscale mode it is tailnet-only, in public mode it exposes "drift has fired N times" to any reader. Same trust class as `/login` and `/cache/...`.
- v1 friend-scope on the slot endpoints (`daydream/api/slots.py`): any auth'd session can create / claim / kick any slot — there is no per-session ownership enforcement on `claim_slot` or `kick_slot`. Explicit in the module docstring and in SPEC 2026-05-07's v1 friend-scope callout; v2 multi-user-shared-world introduces the per-session ownership check.
- `_ensure_session_id` (`daydream/api/auth.py:29-39`) stamps a fresh `uuid.uuid4()` into `request.session` on first read in tailscale mode, even on slot-endpoint hits that did not go through `POST /api/login`. Consistent with the documented "tailnet membership IS the auth" posture; the stamped UUID is the slot picker's controller identity, not an authentication token.
- Request body size on `POST /api/slots/{slot}/create` is unbounded (no `client_max_body_size` / Starlette body-size cap). A tailnet member can POST an arbitrarily large `name` / `appearance_seed` and persist it. Same trust class as the documented friend-scope "any auth'd session can do everything" posture.

---
*Prior review (2026-05-07, paths, commit `63e1ed0`): toon-slot-management landing — five files (`daydream/api/{slots,auth,ws}.py`, `daydream/toons.py`, `daydream/server.py`); four new slot endpoints behind `_require_authed` + `AccessMiddleware`; parameterized SQL throughout; client-side rendering via `escape()` / `textContent` in `web/assets/main.js`; LLM prompt path does not see attacker-controlled toon strings. Three new accepted risks recorded (v1 friend-scope on slot endpoints, `_ensure_session_id` UUID stamping in tailscale mode, unbounded request body size on slot create). 0 BLOCK / 0 WARN / 0 NOTE.*

<!-- SECURITY_META: {"date":"2026-05-07","commit":"dd983fabf5f7ee8f3855f90e06d90d9b626e5099","scope":"paths","block":0,"warn":0,"note":0,"scanned_files":["daydream/admin.py","daydream/llm/bootstrap.py","tests/test_bootstrap.py"]} -->
