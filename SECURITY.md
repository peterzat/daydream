## Security Review — 2026-05-07 (scope: paths)

**Summary:** Review of the 5 paths landing the v0 NPC drift loop (commit `8a3ade4` plus the prior `cc30a20` SPEC consume). The increment introduces a single asyncio.Task that wakes every 5 / 30 minutes (idle / busy cadence), picks one NPC at random from the DB, draws a literal string from a hardcoded `_DRIFT_POOLS` dict in `daydream/drift.py:54-67`, and emits one `narrate` event via `events.append`. The increment introduces no new HTTP endpoint, no new WS message, no LLM call (and therefore no GPU-arbiter contention), no new dependency, and no new external input surface — `_tick` is system-authored: `actor_type="system"`, `actor_id=None` at `daydream/drift.py:120-121`. Pool entries are hardcoded literals (not operator- or player-controlled), and the chosen room comes from the toons table, so the `narrate` text and `room_id` cannot be manipulated by any external actor. SQL in `_list_npcs` (`daydream/drift.py:93-101`) is a literal string with no interpolation; `events.append` (`daydream/events.py:65-69`) parameterizes inserts. No new path-traversal surface (drift never touches the filesystem), no new prompt-injection surface (no LLM call by design — see `drift.py:14-19` rationale and SPEC C3). The `DAYDREAM_DRIFT_ENABLED` / `DAYDREAM_DRIFT_IDLE_SECONDS` / `DAYDREAM_DRIFT_BUSY_SECONDS` env vars are operator-controlled (sit inside the existing `bin/game` env-sourcing accepted-risk class); float coercion of malformed values would `ValueError` at `_compute_next_interval`, surface in the lifespan startup or per-tick log path, and not affect security posture. `daydream/server.py:29-34` lifespan wires `start_drift_loop()` after `db.init_live()` and `stop_drift_loop()` in a `finally` block before `db.close_db()`; if `init_live` raises, drift never starts (correct); if drift's `start` raises, `db.close_db()` still runs in the finally (acceptable, no new exposure). `events.subscriber_count()` (`daydream/events.py:129-132`) is a 3-line public accessor returning `len(_subscribers)`; reading the length is O(1) and safe under asyncio single-threaded execution. The drift task body's bare `except Exception` (`drift.py:144`) logs with `exc_info=True`; the traceback goes through Python logging configured at app level — no new log-injection surface (the line is template-styled). `tests/conftest.py:48` force-sets `DAYDREAM_DRIFT_ENABLED=0` so a default-enabled production behavior cannot leak into tests; `tests/test_drift.py:150` opts in via `monkeypatch.setenv` for the cancellation test only. `tests/conftest.py:21` and `:28` retain the existing `test-session-secret-not-for-production` and `test-password` fixtures (no real credentials; previously accepted). Git history (`git log -p --follow -3`) for all five files shows no secret values introduced or removed. "Rook," "Iris," and the WHIMSY-tone pool lines (forge bell, postcard, ribbon, etc.) are in-fiction NPC content; no PII in scope.

### Findings

No security issues identified.

### Accepted Risks

Unchanged from prior entry (carried forward; none re-flagged this scope):

- Cookie `https_only=False` in `daydream/server.py:42` is documented inline ("friend-scope; box is on a private LAN/Tailscale only").
- No CSRF token on `/api/login` and `/api/logout`. `SessionMiddleware` defaults to `SameSite=Lax`, which blocks cross-site POST from carrying the session cookie; the worst-case residual impact under the documented threat model is forced-logout from a same-site phishing page, consistent with friend-scope posture.
- The 100.64.0.0/10 hardcoding in `daydream/api/access.py` is correct because Tailscale's CGNAT range is fixed by their design. A self-hosted Headscale with a custom range would need that constant updated. Documented in CLAUDE.md.
- `AccessMiddleware` reads `scope["client"][0]` directly rather than honoring forwarded-for headers. This is the secure default given uvicorn is not started with `--proxy-headers`. Operators who later put the app behind a reverse proxy must add `--forwarded-allow-ips` themselves AND audit that the proxy strips inbound `X-Forwarded-For`.
- `bin/game` sources `.env` and `~/.config/daydream/secrets.env` via `set -a; source`; a writable env file is equivalent to arbiter code execution. Operator-controlled, gitignored, standard practice. The new `DAYDREAM_DRIFT_*` env vars (`DAYDREAM_DRIFT_ENABLED`, `DAYDREAM_DRIFT_IDLE_SECONDS`, `DAYDREAM_DRIFT_BUSY_SECONDS`) sit inside this same trust boundary; `DAYDREAM_DRIFT_ENABLED` only toggles a code path, and the seconds vars `float()`-coerce with a `ValueError` on bad input, no security impact.
- `bin/game cmd_logs` passes `$1` through `_log_file` without validation; an operator typing `bin/game logs ../../etc/passwd` would resolve into `$RUNDIR/../../etc/passwd.log`. Operator-invoked only; anything the operator can `tail` through this path they can already read directly on the box.
- `bin/qpeek-bootstrap` clones `https://github.com/peterzat/qpeek` and runs `pip install -e` against it. Supply-chain dependency on the project owner's own GitHub account; same trust boundary as the rest of the dev toolchain.
- `/cache/{world}/{target_kind}/{target_id}/{filename}` in `daydream/server.py:95-107` is intentionally unauthenticated so pre-auth `<img src>` fetches render. `AccessMiddleware` is the real gate; segment validation blocks traversal.
- Tailscale-mode `POST /api/login` at `daydream/api/auth.py` short-circuits to `authed=True` + redirect without checking the password. Deliberate: `AccessMiddleware` has already CGNAT-gated the request, so the password check would be redundant ceremony. Documented in the module docstring and in CLAUDE.md "Auth" + "Network access" sections.
- Data-skill `set_mood` effect handler (`daydream/skills/effects.py`) accepts an LLM-chosen `toon_id` without scoping it to the actor's world / room / ownership. In v0 single-player + friend-scope this sits inside the existing trust boundary. v2's per-effect jsonschema + target-authorization pipeline is tracked under BACKLOG `skills-authoring-and-security`.
- `bin/vllm-bootstrap` interpolates `$MODEL` (sourced from `DAYDREAM_VLLM_MODEL`) into an unquoted heredoc that becomes Python source. A malicious value could escape the `snapshot_download(...)` string and run arbitrary Python in the vLLM venv. Same operator-trust class as the existing `bin/game` env-sourcing accepted risk.

---
*Prior review (2026-05-06, paths, commit b6a2551): scope was the cleanup-hygiene round (gguf `__version__` patch, `DAYDREAM_VLLM_MAX_LEN` rename) plus second-NPC turn (Iris seed migration, `skills/iris.json`, supporting tests). Iris reuses Rook's safety pipeline (banlist, role-separator wrap, `SandboxedEnvironment` render, refusal parse, output banlist, effect allowlist); migration 008 is literal SQL with no operator input flow; gguf-patch idempotency verified; `bin/vllm-bootstrap` `$MODEL` heredoc interpolation recorded under operator-trust accepted risks. No findings.*

<!-- SECURITY_META: {"date":"2026-05-07","commit":"c94a16e4a8d56efa02ae0f3592532d600d119a08","scope":"paths","block":0,"warn":0,"note":0,"scanned_files":["daydream/drift.py","daydream/events.py","daydream/server.py","tests/conftest.py","tests/test_drift.py"]} -->
