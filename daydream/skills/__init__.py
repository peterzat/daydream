"""Skills: context-sensitive actions a toon can take.

Core skills (this package) are baked-in Python handlers. Data skills (v1)
will be DB rows with Jinja prompt templates and an effects allowlist; both
kinds compose through the same registry.
"""
